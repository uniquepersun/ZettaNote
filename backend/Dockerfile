FROM node:24
WORKDIR .

COPY package*.json ./
RUN npm install
COPY . .

ENV PORT=BACKEND_PORT
ENV DB=MONGODB_URL
ENV JWT_SECRET=RANDOM_SECRET_KEY

EXPOSE [SAME AS BACKEND_PORT]

CMD ["npm", "run", "prod"]
